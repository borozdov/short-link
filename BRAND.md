# BOROZDOV — Брендбук

**Архетип: Правитель.** Власть. Доминантность. Сила. Уверенность.
**Два лика одной власти:** ОБСИДИАН (тьма) и ТИТАН (гербовая бумага).

---

## 1. Пять законов

1. **Один материал — монохром. Два лика.** Ноль оттенка во всей системе — только серая шкала. У власти два лика: **ОБСИДИАН** — тьма, монолит, ночь (первичный, внешний лик бренда) и **ТИТАН** — гербовая бумага, чернильная литера, приказ. Светлая тема — не «смягчение», а тот же характер в другом материале: ничего кремового, тёплого или пастельного.
2. **Инверсия — единственный акцент.** Выделить — значит залить противоположным полюсом: в Обсидиане — титановая плашка с обсидиановым текстом, в Титане — обсидиановая «печать» с титановым текстом. Никаких акцентных цветов, градиентов и цветных подложек — ни в одном из ликов.
3. **Прописные — голос бренда.** Заголовки и лейблы — UPPERCASE. Заголовки сжаты (tracking −0.02em), лейблы разрежены (0.08–0.16em). Интонация приказа, а не просьбы.
4. **Линии вместо теней.** Глубина строится 1px hairline-границами и ступенями поверхности. В Титане это критично вдвойне: карточки отделяются только границей — никаких «мягких» теней.
5. **Тема следует за средой.** По умолчанию лик выбирается по окружению пользователя (светлое/тёмное), поверх — ручной выбор с памятью. Бренд диктует материал, пользователь выбирает освещение.

---

## 2. Палитра

### 2.1. Роли и значения

Значения ликов — **не наивная инверсия**: `slate`/`border-strong` подобраны отдельно под физику контраста на белом (вторичный текст в Титане темнее середины шкалы; интерактивная граница светлее вторичного текста).

| Роль | ОБСИДИАН (тёмный) | ТИТАН (светлый) | Назначение |
|---|---|---|---|
| `canvas` | `#0d0d0d` | `#fafafa` | Канва страницы; текст инверсии |
| `inset` | `#121212` | `#ececec` | Утопленное: инпуты, шапки таблиц, колодцы |
| `surface` | `#1a1a1a` | `#ffffff` | Приподнятое: карточки, панели, меню |
| `hover` | `#212121` | `#f2f2f2` | Ховер поверхностей и строк таблиц |
| `zebra` | `#161616` | `#f6f6f6` | Чередование строк в графических экспортах |
| `border` | `#2e2e2e` | `#e4e4e4` | Декоративный hairline 1px |
| `border-strong` | `#6b6b6b` | `#8a8a8a` | Интерактивные границы; контурные бейджи |
| `slate` | `#8a8a8a` | `#6b6b6b` | Вторичный текст, лейблы |
| `tertiary` | `#6b6b6b` | `#8a8a8a` | Третичный текст, плейсхолдеры |
| `soft` | `#d1d1d1` | `#3d3d3d` | Подчёркнутый вторичный текст |
| `text` | `#fafafa` | `#0d0d0d` | Основной текст; заливка инверсии |
| `focus ring` | `rgba(250,250,250,.22)` | `rgba(13,13,13,.22)` | Кольцо фокуса 3px |
| `scrim` | `rgba(0,0,0,.7)` | `rgba(13,13,13,.45)` | Подложка модалок (+blur) |

### 2.2. Правило зеркала

Система двухполюсная: **полюс канвы** и **полюс текста**. При смене лика полюса меняются местами, отсюда два тождества, на которых держится вся система:

- **заливка инверсии ≡ цвету текста** (`text`);
- **текст инверсии ≡ цвету канвы** (`canvas`).

Направление «приподнятости» следует за светом: в Обсидиане surface **светлее** канвы, в Титане — **белее** канвы. Hover всегда сдвигается к текстовому полюсу.

**Выделение текста (selection):** инверсия в обоих ликах.

### 2.3. Запрещено

Любой цветной оттенок (синий, фиолетовый, зелёный, золотой…) · градиенты · цветные полупрозрачные подложки · пилюли (`border-radius: 999px`) · тени как способ построения глубины · эмодзи в интерфейсе · **смешение ликов** (светлая карточка на тёмной канве и наоборот — лик всегда один на всю страницу).

Закрытый список исключений — §4.

---

## 3. Контрасты WCAG

Все пары посчитаны по формуле относительной яркости WCAG 2.x.

### 3.1. ОБСИДИАН

| Пара | Контраст | Статус |
|---|---|---|
| `text #fafafa` на `canvas #0d0d0d` | 18.6:1 | AAA |
| `text #fafafa` на `surface #1a1a1a` | 16.7:1 | AAA |
| `soft #d1d1d1` на `canvas` | 12.7:1 | AAA |
| `slate #8a8a8a` на `canvas` | 5.6:1 | AA |
| `slate #8a8a8a` на `surface` | 5.0:1 | AA |
| `tertiary #6b6b6b` на `canvas` | 3.6:1 | только UI / крупный текст |
| `border-strong #6b6b6b` к `canvas` | 3.6:1 | UI ≥3:1 ✓ |

### 3.2. ТИТАН

| Пара | Контраст | Статус |
|---|---|---|
| `text #0d0d0d` на `canvas #fafafa` | 18.6:1 | AAA |
| `text #0d0d0d` на `surface #ffffff` | 19.4:1 | AAA |
| `soft #3d3d3d` на `canvas` | 10.4:1 | AAA |
| `slate #6b6b6b` на `canvas` | 5.1:1 | AA |
| `slate #6b6b6b` на `surface` | 5.3:1 | AA |
| `tertiary #8a8a8a` на `canvas` | 3.3:1 | только UI / крупный текст |
| `border-strong #8a8a8a` к `canvas` | 3.3:1 | UI ≥3:1 ✓ |

### 3.3. Правила применения

- Текст мельче 18.5px/24px — минимум **AA (4.5:1)**: это `text`, `soft`, `slate`.
- `tertiary`/`border-strong` — **не для мелкого текста**: только плейсхолдеры-подсказки, крупные цифры, границы и прочий UI (порог 3:1).
- Инверсионные плашки в обоих ликах дают 18.6:1 — на них разрешён текст любого кегля.

---

## 4. Что следует лику, а что — константа

**Следуют выбранному лику:** весь интерфейс, генерируемые картинки для шеринга и экспорта, экраны загрузки.

**Константы вне зеркала (закрытый список):**

| Артефакт | Значение | Причина |
|---|---|---|
| QR-код | модули `#0d0d0d` на плашке `#fafafa`; в Титане плашка получает рамку `border` | Инвертированные QR читаются не всеми сканерами |
| Печать | чёрным по белому, grayscale | Бумага — не экран |
| Иконки приложений, внешние превью-карточки | обсидиан `#0d0d0d` | Внешний лик бренда — тёмный |
| Фавиконы | титановый квадрат (rx 16%) + обсидиановая литера | Читаются на любом фоне |

---

## 5. Типографика

Неизменна между ликами — меняются только цвета.

| Роль | Шрифт | Правила |
|---|---|---|
| Заголовки | **Inter** 600 | UPPERCASE, tracking −0.02em, крупный кегль |
| Текст | **Inter** 400/500 | Обычный регистр |
| Лейблы | **Inter** 500 | UPPERCASE, 10–12px, tracking 0.08–0.16em, цвет `slate` |
| Цифры: время, очки, рекорды, даты | **JetBrains Mono** (fallback: Berkeley Mono, ui-monospace) | `tabular-nums`, вес 400–700 |

- Где внешние шрифты недоступны — системный стек с той же иерархией.
- Цифры результатов — всегда моно с табличными цифрами. Результат — главный герой.

---

## 6. Геометрия

- **Радиусы:** 2 / 4 / 6 / 8 px, максимум 8. Никаких пилюль и кругов, кроме точек-индикаторов и лоадеров.
- **Границы:** 1px; декоративные — `border`, интерактивные — `border-strong`.
- **Тени:** нет. Единственная «глубина поверх» — `scrim` + backdrop-blur у модалок.
- **Сетка:** просторные отступы; секции разделяются hairline-линиями во всю ширину.

---

## 7. Компоненты — рецепты в ролях

Рецепты записаны в ролях (§2.1), поэтому они **одни и те же в обоих ликах** — меняются только значения ролей.

| Компонент | Рецепт |
|---|---|
| Кнопка primary | заливка `text`, текст `canvas`, вес 600; hover — `brightness(0.85)` |
| Кнопка secondary | прозрачная, граница `border`, текст `text`, UPPERCASE 11–12px tracking 0.08em; hover — граница `border-strong` |
| Чип / сегмент активный | инверсия (как primary), 600 |
| Чип неактивный | прозрачный, граница `border`, текст `slate`; hover — текст `text` |
| Переключатель лика | штриховые SVG солнце/луна (stroke 2, скруглённые концы); иконка показывает, **куда** переключит клик: в тёмном лике — солнце, в светлом — луна |
| Инпут | фон `inset`, граница `border`; фокус — граница `border-strong` + focus ring |
| Карточка | фон `surface`, граница `border`, радиус 8 |
| Таблица | шапка: фон `inset`, текст `slate` UPPERCASE tracking 0.12em; ховер строки `hover`; результат — моно `text` |
| Бейдж «новое / рекорд» | инверсия, радиус 2, UPPERCASE |
| Бейдж вторичный | контур `border-strong`, текст `soft`, прозрачный фон |
| Тост | инверсия |
| Скроллбар | трек `canvas`, ползунок `border-strong`, hover `text` |
| Модалка | `surface` + граница `border`; подложка `scrim` + blur |
| Экспорт-картинка | канва `canvas`, карта `surface` с hairline, шапка `inset`, зебра `zebra`, главная цифра — инверсия |

---

## 8. Движение

- Интерактив: 0.15s ease. Появление: 0.3s ease (fade + 4–20px подъём).
- Hover-жест бренда — инверсия (перелив в заливку текстовым полюсом).
- Смена лика мгновенна (без transition-каскада на цветах).
- `prefers-reduced-motion` уважается всегда.

---

## 9. Голос

- Кратко, категорично, без украшений. «ВЛАСТЬ. РЕЗУЛЬТАТ.»
- Без эмодзи в интерфейсе. Иконки — только штриховые SVG (stroke 2–2.5, `currentColor`).
- Подпись: `BY BOROZDOV` — UPPERCASE, `slate`, tracking 0.12em.

---

*Канон един для всей экосистемы; копии этого файла обязаны быть идентичными.*
